#include <stdio.h>

int main(){
    
    int nX = 1;
    int nY = 1;
    nY = nX + 1;        //1+1=2
    nX = 1 + nX + nY;   //1+1+2=4
    nY = 2 * nX ++ * (nY = nX * 3) + nX ++ + nY --;
    //2*4*(5*3)+6+14=140
    printf("nX = %d, nY = %d\n", nX, nY);
    return 0;    
}

